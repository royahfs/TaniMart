<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaniMart">

        <!-- Splash (Launcher) -->
        <activity
            android:name=".ui.common.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login & Signup -->
        <activity android:name=".ui.common.login.LoginActivity" android:exported="true" />
        <activity android:name=".ui.common.signup.SignUpActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Dashboard (multi user) -->
        <activity android:name=".ui.admin.dashboard.AdminDashboardActivity" android:exported="true" />
        <activity android:name=".ui.kasir.dashboard.KasirDashboardActivity" android:exported="true" />
        <activity android:name=".ui.karyawan.dashboard.KaryawanDashboardActivity" android:exported="true" />

        <!-- Inventory -->
        <activity android:name=".ui.common.inventory.InventoryActivity" android:exported="true" />
        <activity android:name=".ui.common.inventory.DaftarProdukActivity" android:exported="true" />
        <activity android:name=".ui.common.inventory.DetailProdukActivity" android:exported="true" />
        <activity android:name=".ui.common.inventory.KategoriProdukActivity" android:exported="true" />
        <activity android:name=".ui.common.inventory.KelolaProdukMasukActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ui.common.inventory.ProdukKeluarActivity" android:exported="true" />

        <!-- Kasir & Transaksi -->
        <activity android:name=".ui.kasir.transaksi.TransaksiActivity" android:exported="true" />
        <activity android:name=".ui.kasir.transaksi.PembayaranActivity" android:exported="true" />
        <activity android:name=".ui.kasir.transaksi.pembayaran.PembayaranTunaiActivity" android:exported="false" />
        <activity android:name=".ui.kasir.transaksi.pembayaran.DialogTransaksiBerhasilActivity" android:exported="false" />
        <activity android:name=".ui.kasir.transaksi.pembayaran.ReceiptPrintActivity" android:exported="false" />
        <activity android:name=".ui.kasir.laporan.LaporanActivity" android:exported="true" />
        <activity android:name=".ui.kasir.transaksi.CartActivity" android:exported="false" />
        <activity android:name=".ui.kasir.transaksi.pembayaran.PembayaranQrisActivity" android:exported="false"/>
        <activity android:name=".ui.kasir.transaksi.pembayaran.PembayaranTransferActivity" android:exported="false"/>

        <!-- Kalkulator -->
        <activity android:name=".ui.common.kalkulator.KalkulatorActivity" android:exported="true" />

        <!-- Profil -->
        <activity android:name=".ui.common.profile.ProfileActivity" android:exported="false" />
        <activity android:name=".ui.common.profile.EditProfileActivity" android:exported="false" />

        <!-- Root -->
        <activity android:name=".MainActivity" android:exported="true" />
    </application>

</manifest>
