<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Warna utama tombol -->
    <solid android:color="@color/dark_teal_sage" />

    <!-- Sudut membulat -->
    <corners android:radius="12dp" />

    <!-- Sedikit bayangan untuk efek 3D -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />

    <size android:height="50dp" />

    <stroke
        android:width="1dp"
        android:color="@color/dark_teal_sage" />
</shape>
